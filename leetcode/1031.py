class Solution:
    def numEnclaves(self, A: list) -> int:

        for i in range(len(A)):
            pass

        for i in range(len(A)):

            for j in range(len(A[0])):
                pass